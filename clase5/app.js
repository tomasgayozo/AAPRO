/* contar cuantos numers pares hay del 1 al 50: usar bucle y contador

let contador=0;

for (let numero = 1; numero <= 50; numero++) 
    {
        if (numero%2===0)
            contador ++;
        


    }

    console.log("cantidad de pares entre 1 al 50 es:"+contador);
    */

// setTimeout(function, delay)
//var delay = 1000 // 1000ms o 1s
//setTimeout( () => {}, delay ) // Utilizando variable para el delay
//setTimeout( () => {}, 1000 ) // Declarando el delay en el setTimeout

/*
function funcionArray() {
    // codigo
}
var arrayVariable = [1, "hola", null, {}, funcionArray]
*/

//for (/* Donde comienza */; /* Hasta donde llega */; /* Manera de progresar */) {
    // estructura a repetir
//}

//var arrayConNombres = ["fausto", "nico", "agus", "fran"];
//for (let index = 0; index < arrayConNombres.length; index++) {
  //  var nombreParticular = arrayConNombres[index];
 //   console.log(nombreParticular);
//}

/*
var arrayConNombres = ["fausto", "nico", "agus", "fran"];
arrayConNombres.forEach(nombre => {
    console.log(nombre);
});
*/
